import { ResizableBar } from "@/components/resizeable-bar-comp/resizeable-bar";
import React from "react";

const page = () => {
  return (
    <div>
      <ResizableBar />
    </div>
  );
};

export default page;
